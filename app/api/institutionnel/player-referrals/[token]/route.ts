import {NextResponse} from "next/server";
import {createAdminClient} from "@/lib/supabase/admin-server";

export async function GET(_req:Request,{params}:{params:Promise<{token:string}>}){const{token}=await params;const db=createAdminClient();if(!db)return NextResponse.json({error:"Service indisponible"},{status:503});const{data,error}=await db.from("institutional_structures").select("id,name,short_name,logo_url,document_primary_color,document_secondary_color").eq("player_referral_token",token).eq("archived",false).maybeSingle();if(error||!data)return NextResponse.json({error:"Lien invalide"},{status:404});return NextResponse.json({structure:data})}
export async function POST(req:Request,{params}:{params:Promise<{token:string}>}){const{token}=await params;const db=createAdminClient();if(!db)return NextResponse.json({error:"Service indisponible"},{status:503});const{data:s}=await db.from("institutional_structures").select("id,name").eq("player_referral_token",token).eq("archived",false).maybeSingle();if(!s)return NextResponse.json({error:"Lien invalide"},{status:404});const b=await req.json().catch(()=>({}));const first=String(b.first_name||"").trim(),last=String(b.last_name||"").trim(),reporterEmail=String(b.reporter_email||"").trim();if(!first||!last||!reporterEmail)return NextResponse.json({error:"Prénom, nom du joueur et email du signalant obligatoires."},{status:400});const q=await db.from("institutional_player_referrals").insert({structure_id:s.id,first_name:first,last_name:last,birthdate:b.birthdate||null,club_name:String(b.club_name||"").trim()||null,category:String(b.category||"").trim()||null,reporter_name:String(b.reporter_name||"").trim()||null,reporter_email:reporterEmail,reporter_phone:String(b.reporter_phone||"").trim()||null,reason:String(b.reason||"").trim()||null});if(q.error)return NextResponse.json({error:q.error.message},{status:400});return NextResponse.json({ok:true})}
