"use client";import {useParams,useRouter} from "next/navigation";import SessionSelfEvaluation from "@/components/evaluations/SessionSelfEvaluation";export default function SeanceBilanPage(){const params=useParams<{id:string}>();const router=useRouter();const id=String(params?.id||'');return <main style={{minHeight:'100vh',background:'#faf7f3'}}><div style={{maxWidth:1180,margin:'0 auto',padding:'18px 26px 0'}}><button onClick={()=>router.push(`/seances/${id}`)} style={{border:0,borderRadius:999,padding:'9px 14px',background:'#f4e8dc',color:'#6b1a2c',fontWeight:900,cursor:'pointer'}}>← Retour séance</button></div>{id&&<SessionSelfEvaluation sessionId={id}/>}</main>}
