'use client';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';import{createDocument,listDocuments}from'@/lib/workspace/documents';import type{DocumentKind}from'@/lib/workspace/types';
export default function Page(){const r=useRouter();const[docs,setDocs]=useState<any[]>([]);async function load(){setDocs(await listDocuments())}useEffect(()=>{load()},[]);async function add(){const title=prompt('Nom du document ?');if(!title)return;const kind=(prompt('Type : playbook / exercise_book / session / program / mixed','mixed')||'mixed') as DocumentKind;const d=await createDocument(title,kind);r.push(`/mon-compte/documents/${d.id}`)}return <main style={{padding:32,maxWidth:1100,margin:'auto'}}><div style={{display:'flex',justifyContent:'space-between'}}><div><h1>Document Builder</h1><p>Playbooks, cahiers d’exercices, séances, programmes et documents mixtes.</p></div><button onClick={add}>+ Nouveau document</button></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(260px,1fr))',gap:16}}>{docs.map(d=><button key={d.id} onClick={()=>r.push(`/mon-compte/documents/${d.id}`)} style={{padding:22,textAlign:'left',border:'1px solid #ddd',borderRadius:12,background:'white'}}><b>{d.title}</b><div>{d.kind}</div><small>{d.season||'Toutes saisons'}</small></button>)}</div></main>}
