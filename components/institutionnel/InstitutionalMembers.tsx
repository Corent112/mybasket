"use client";
import {useEffect,useState} from "react";
type M={id:string;role:string;status:string;permissions:any;profiles?:{display_name?:string;email?:string}};
const roles=["admin","formation","performance","communication","lecture"];
export default function InstitutionalMembers({structureId}:{structureId:string}){const[rows,setRows]=useState<M[]>([]),[email,setEmail]=useState(""),[role,setRole]=useState("lecture"),[busy,setBusy]=useState(false);async function load(){const r=await fetch(`/api/institutionnel/members?structureId=${encodeURIComponent(structureId)}`,{cache:"no-store"});const j=await r.json();if(r.ok)setRows(j.members||[])}useEffect(()=>{void load()},[structureId]);async function invite(){if(!email.includes("@"))return alert("Email invalide");setBusy(true);const r=await fetch("/api/institutionnel/members",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({structureId,email,role})});const j=await r.json();setBusy(false);if(!r.ok)return alert(j.error||"Invitation impossible");setEmail("");await load();alert("Invitation envoyée")}async function patch(id:string,nextRole:string){await fetch("/api/institutionnel/members",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({structureId,memberId:id,role:nextRole})});await load()}return <div className="members"><div className="invite"><div><small>AJOUTER UN MEMBRE</small><h3>Inviter un collaborateur</h3><p>Le membre reçoit un compte / une invitation MyBasket et ses droits restent limités à cette structure.</p></div><div className="inviteForm"><input type="email" placeholder="prenom.nom@email.fr" value={email} onChange={e=>setEmail(e.target.value)}/><select value={role} onChange={e=>setRole(e.target.value)}>{roles.map(x=><option key={x} value={x}>{x}</option>)}</select><button disabled={busy} onClick={invite}>{busy?"Invitation…":"Inviter"}</button></div></div><div className="memberGrid">{rows.map(m=><article key={m.id}><div className="avatar">{(m.profiles?.display_name||m.profiles?.email||"M")[0]?.toUpperCase()}</div><div><b>{m.profiles?.display_name||"Membre"}</b><span>{m.profiles?.email||"Compte MyBasket"}</span><small>{m.status}</small></div><select value={m.role} onChange={e=>patch(m.id,e.target.value)}>{roles.concat(["owner"]).map(x=><option key={x}>{x}</option>)}</select></article>)}</div><style jsx>{`.members{display:grid;gap:14px}.invite{display:grid;grid-template-columns:1fr minmax(360px,1fr);gap:20px;padding:18px;border-radius:16px;background:linear-gradient(135deg,#fff9ed,#fff);border:1px solid #ead7b2}.invite small{color:#b17a21;font-weight:1000;letter-spacing:.12em}.invite h3{margin:4px 0;color:#4d1420}.invite p{margin:0;color:#7d6e66;font-size:.8rem}.inviteForm{display:grid;grid-template-columns:1fr 140px auto;gap:7px;align-content:center}.inviteForm input,.inviteForm select,.memberGrid select{border:1px solid #ddcfc8;border-radius:10px;padding:10px;background:white}.inviteForm button{border:0;border-radius:10px;background:#6b1a2c;color:white;font-weight:900;padding:10px 15px}.memberGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.memberGrid article{display:grid;grid-template-columns:44px 1fr 130px;gap:10px;align-items:center;border:1px solid #eadfd9;border-radius:13px;padding:12px}.avatar{width:44px;height:44px;border-radius:50%;display:grid;place-items:center;background:#6b1a2c;color:white;font-weight:1000}.memberGrid b,.memberGrid span,.memberGrid small{display:block}.memberGrid span{font-size:.76rem;color:#776a64}.memberGrid small{font-size:.65rem;color:#b17a21;text-transform:uppercase;font-weight:900}@media(max-width:800px){.invite,.memberGrid{grid-template-columns:1fr}.inviteForm{grid-template-columns:1fr}.memberGrid article{grid-template-columns:44px 1fr}.memberGrid select{grid-column:1/-1}}`}</style></div>}
