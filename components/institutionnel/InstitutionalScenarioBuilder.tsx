"use client";import {useEffect,useMemo,useState} from "react";import {createClient} from "@/lib/supabase/client";
type Block={id:string;title:string;event_type?:string;location?:string|null;intervenant?:string|null;start_time?:string|null;end_time?:string|null};
export default function InstitutionalScenarioBuilder({structureId}:{structureId:string}){const s=useMemo(()=>createClient(),[]),[blocks,setBlocks]=useState<Block[]>([]),[sequence,setSequence]=useState<Block[]>([]),[title,setTitle]=useState("Scénario pédagogique"),[saved,setSaved]=useState(false);useEffect(()=>{void(async()=>{const q=await s.from("institutional_events").select("id,title,event_type,location,intervenant,start_time,end_time").eq("structure_id",structureId).eq("archived",false).order("event_date",{ascending:false}).limit(100);setBlocks((q.data||[]) as Block[])})()},[structureId]);function drop(e:React.DragEvent){const id=e.dataTransfer.getData("text/plain");const b=blocks.find(x=>x.id===id);if(b)setSequence(v=>[...v,{...b,id:`${b.id}-${Date.now()}`}])}async function save(){const{data:{user}}=await s.auth.getUser();if(!user)return;const q=await s.from("institutional_documents").insert({structure_id:structureId,title,document_type:"pedagogical_scenario",content:{blocks:sequence},created_by:user.id});if(q.error)return alert(q.error.message);setSaved(true);setTimeout(()=>setSaved(false),1500)}return <div className="sc"><div className="head"><div><small>SCÉNARIO PÉDAGOGIQUE</small><h3>Construire à partir de blocs déjà créés</h3></div><input value={title} onChange={e=>setTitle(e.target.value)}/></div><div className="cols"><section><b>Blocs disponibles</b><p>Glisse les séances, interventions ou séquences déjà créées.</p><div className="bank">{blocks.map(b=><article draggable onDragStart={e=>e.dataTransfer.setData("text/plain",b.id)} key={b.id}><span>⋮⋮</span><div><b>{b.title}</b><small>{b.event_type||"bloc"} · {b.intervenant||"intervenant à définir"}</small></div></article>)}</div></section><section className="drop" onDragOver={e=>e.preventDefault()} onDrop={drop}><b>Déroulé pédagogique</b><p>Dépose les blocs ici puis réorganise-les.</p>{sequence.map((b,i)=><article key={b.id}><em>{i+1}</em><div><b>{b.title}</b><small>{b.start_time?.slice(0,5)||"--:--"} - {b.end_time?.slice(0,5)||"--:--"} · {b.location||"lieu à définir"}</small></div><div><button disabled={i===0} onClick={()=>setSequence(v=>{const n=[...v];[n[i-1],n[i]]=[n[i],n[i-1]];return n})}>↑</button><button disabled={i===sequence.length-1} onClick={()=>setSequence(v=>{const n=[...v];[n[i+1],n[i]]=[n[i],n[i+1]];return n})}>↓</button><button onClick={()=>setSequence(v=>v.filter((_,j)=>j!==i))}>×</button></div></article>)}{!sequence.length&&<div className="empty">Dépose un premier bloc ici</div>}</section></div><div className="actions"><button onClick={()=>setSequence([])} className="ghost">Vider</button><button onClick={save}>💾 {saved?"Enregistré":"Enregistrer le scénario"}</button></div><style jsx>{`.sc{display:grid;gap:12px;margin-top:16px;padding-top:16px;border-top:1px solid #eadfd8}.head{display:flex;justify-content:space-between;gap:12px;align-items:center}.head small{color:#b37a20;font-weight:1000;letter-spacing:.1em}.head h3{margin:3px 0;color:#4d1420}.head input{min-width:280px;border:1px solid #ddcfc8;border-radius:10px;padding:10px;font-weight:800}.cols{display:grid;grid-template-columns:1fr 1.3fr;gap:12px}.cols>section{border:1px solid #eadfd8;border-radius:14px;padding:12px;background:#faf8f6}.cols>section>p{font-size:.75rem;color:#7d7069}.bank,.drop{display:grid;gap:7px}.bank article,.drop article{display:grid;grid-template-columns:auto 1fr auto;gap:9px;align-items:center;background:white;border:1px solid #e6dad4;border-radius:10px;padding:9px}.bank article{cursor:grab}.bank span{color:#a9968e}.bank small,.drop small{display:block;color:#7c6e67;font-size:.7rem}.drop em{width:28px;height:28px;border-radius:50%;display:grid;place-items:center;background:#6b1a2c;color:white;font-style:normal;font-weight:900}.drop button{padding:5px 7px}.empty{border:2px dashed #dbc8be;border-radius:12px;padding:30px;text-align:center;color:#9a8a82;background:white}.actions{display:flex;justify-content:flex-end;gap:7px}.actions button,.drop button{border:0;border-radius:8px;background:#6b1a2c;color:white;font-weight:900}.actions button{padding:9px 12px}.actions .ghost{background:white;color:#6b1a2c;border:1px solid #d9c2c8}@media(max-width:800px){.cols{grid-template-columns:1fr}.head{align-items:flex-start;flex-direction:column}.head input{width:100%;min-width:0;box-sizing:border-box}}`}</style></div>}
